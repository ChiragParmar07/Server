export enum GENDER {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum USER_STATUS {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  DELETED = 'Deleted',
}

export const gender = ['Male', 'Female'];
