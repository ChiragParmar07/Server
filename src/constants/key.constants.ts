export enum GENDER {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum ROLES {
  ADMIN = 'admin',
  USER = 'user',
}

export enum USER_STATUS {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  DELETED = 'Deleted',
}

export const gender = ['Male', 'Female'];
